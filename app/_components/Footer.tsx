import { Section } from "./Section";

type Props = {};

export const Footer = (props: Props) => {
  return (
    <footer className="bg-card">
      <Section>@ Copyright Popol 2024</Section>
    </footer>
  );
};
